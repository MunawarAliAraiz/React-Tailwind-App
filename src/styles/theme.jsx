// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#000', // Change this to your desired primary color (black)
    },
    secondary: {
      main: '#00ff00', // Change this to your desired secondary color (neon green)
    },
    text: {
      primary: '#fff', // Change this to your desired text color (white)
    },
  },
});

export default theme;


